import { sql } from "drizzle-orm";
import { boolean, check, index, integer, jsonb, numeric, pgEnum, pgTable, primaryKey, text, timestamp, uniqueIndex, uuid, varchar } from "drizzle-orm/pg-core";

const id = () => uuid("id").defaultRandom().primaryKey();
const createdAt = () => timestamp("created_at", { withTimezone: true }).defaultNow().notNull();
const updatedAt = () => timestamp("updated_at", { withTimezone: true }).defaultNow().notNull();
export const userStatus = pgEnum("user_status", ["PENDING_VERIFICATION","ACTIVE","SUSPENDED","DELETED"]);
export const contentStatus = pgEnum("content_status", ["DRAFT","IN_REVIEW","PUBLISHED","ARCHIVED"]);
export const questionType = pgEnum("question_type", ["SINGLE_CHOICE","MULTIPLE_CHOICE","NUMERIC","TEXT"]);
export const testMode = pgEnum("test_mode", ["PRACTICE","MOCK","LIVE"]);
export const attemptStatus = pgEnum("attempt_status", ["CREATED","IN_PROGRESS","SUBMITTED","AUTO_SUBMITTED","EVALUATED","VOID"]);
export const orderStatus = pgEnum("order_status", ["CREATED","PENDING","PAID","FAILED","CANCELLED","REFUNDED","PARTIALLY_REFUNDED"]);
export const paymentStatus = pgEnum("payment_status", ["CREATED","AUTHORIZED","CAPTURED","FAILED","REFUNDED","PARTIALLY_REFUNDED"]);
export const entitlementStatus = pgEnum("entitlement_status", ["ACTIVE","EXPIRED","REVOKED","REFUNDED"]);
export const materialType = pgEnum("material_type", ["ARTICLE","PDF","VIDEO","FILE"]);

export const users = pgTable("users", { id:id(), email:varchar("email",{length:320}).notNull(), phone:varchar("phone",{length:20}), passwordHash:text("password_hash").notNull(), status:userStatus("status").default("PENDING_VERIFICATION").notNull(), name:varchar("name",{length:120}).notNull(), emailVerifiedAt:timestamp("email_verified_at",{withTimezone:true}), lastLoginAt:timestamp("last_login_at",{withTimezone:true}), createdAt:createdAt(), updatedAt:updatedAt() }, t=>[uniqueIndex("users_email_uq").on(t.email)]);
export const sessions = pgTable("sessions", { id:id(), userId:uuid("user_id").references(()=>users.id,{onDelete:"cascade"}).notNull(), tokenHash:text("token_hash").notNull(), ipHash:text("ip_hash"), userAgent:text("user_agent"), expiresAt:timestamp("expires_at",{withTimezone:true}).notNull(), revokedAt:timestamp("revoked_at",{withTimezone:true}), createdAt:createdAt() }, t=>[uniqueIndex("sessions_token_hash_uq").on(t.tokenHash),index("sessions_user_idx").on(t.userId)]);
export const verificationTokens = pgTable("verification_tokens", { id:id(), userId:uuid("user_id").references(()=>users.id,{onDelete:"cascade"}).notNull(), purpose:varchar("purpose",{length:30}).notNull(), tokenHash:text("token_hash").notNull(), expiresAt:timestamp("expires_at",{withTimezone:true}).notNull(), consumedAt:timestamp("consumed_at",{withTimezone:true}), createdAt:createdAt() },t=>[uniqueIndex("verification_token_uq").on(t.tokenHash)]);
export const exams = pgTable("exams", { id:id(), slug:varchar("slug",{length:160}).notNull(), name:varchar("name",{length:180}).notNull(), description:text("description"), status:contentStatus("status").default("DRAFT").notNull(), createdBy:uuid("created_by").references(()=>users.id), createdAt:createdAt(), updatedAt:updatedAt() },t=>[uniqueIndex("exams_slug_uq").on(t.slug)]);
export const subjects = pgTable("subjects", { id:id(), examId:uuid("exam_id").references(()=>exams.id,{onDelete:"cascade"}).notNull(), name:varchar("name",{length:160}).notNull(), sortOrder:integer("sort_order").default(0).notNull(), createdAt:createdAt() },t=>[uniqueIndex("subjects_exam_name_uq").on(t.examId,t.name)]);
export const topics = pgTable("topics", { id:id(), subjectId:uuid("subject_id").references(()=>subjects.id,{onDelete:"cascade"}).notNull(), name:varchar("name",{length:160}).notNull(), sortOrder:integer("sort_order").default(0).notNull(), createdAt:createdAt() },t=>[uniqueIndex("topics_subject_name_uq").on(t.subjectId,t.name)]);
export const questions = pgTable("questions", { id:id(), topicId:uuid("topic_id").references(()=>topics.id).notNull(), type:questionType("type").notNull(), stem:text("stem").notNull(), explanation:text("explanation"), marks:numeric("marks",{precision:8,scale:2}).default("1").notNull(), negativeMarks:numeric("negative_marks",{precision:8,scale:2}).default("0").notNull(), difficulty:varchar("difficulty",{length:20}).default("MEDIUM").notNull(), status:contentStatus("status").default("DRAFT").notNull(), createdBy:uuid("created_by").references(()=>users.id).notNull(), reviewedBy:uuid("reviewed_by").references(()=>users.id), publishedAt:timestamp("published_at",{withTimezone:true}), createdAt:createdAt(), updatedAt:updatedAt() },t=>[index("questions_topic_status_idx").on(t.topicId,t.status),check("questions_marks_check",sql`${t.marks} > 0 and ${t.negativeMarks} >= 0`)]);
export const questionOptions = pgTable("question_options", { id:id(), questionId:uuid("question_id").references(()=>questions.id,{onDelete:"cascade"}).notNull(), body:text("body").notNull(), isCorrect:boolean("is_correct").default(false).notNull(), sortOrder:integer("sort_order").notNull() },t=>[uniqueIndex("question_options_order_uq").on(t.questionId,t.sortOrder)]);
export const products = pgTable("products", { id:id(), slug:varchar("slug",{length:160}).notNull(), name:varchar("name",{length:180}).notNull(), description:text("description"), pricePaise:integer("price_paise").notNull(), accessDays:integer("access_days").notNull(), status:contentStatus("status").default("DRAFT").notNull(), createdAt:createdAt(), updatedAt:updatedAt() },t=>[uniqueIndex("products_slug_uq").on(t.slug),check("products_price_access_check",sql`${t.pricePaise} >= 0 and ${t.accessDays} > 0`)]);
export const tests = pgTable("tests", { id:id(), examId:uuid("exam_id").references(()=>exams.id).notNull(), title:varchar("title",{length:200}).notNull(), mode:testMode("mode").notNull(), durationMinutes:integer("duration_minutes").notNull(), instructions:text("instructions"), maxAttempts:integer("max_attempts").default(1).notNull(), shuffleQuestions:boolean("shuffle_questions").default(true).notNull(), shuffleOptions:boolean("shuffle_options").default(true).notNull(), status:contentStatus("status").default("DRAFT").notNull(), createdAt:createdAt(), updatedAt:updatedAt() },t=>[index("tests_mode_status_idx").on(t.mode,t.status),check("tests_duration_attempts_check",sql`${t.durationMinutes} > 0 and ${t.maxAttempts} > 0`)]);
export const testSections = pgTable("test_sections", { id:id(), testId:uuid("test_id").references(()=>tests.id,{onDelete:"cascade"}).notNull(), title:varchar("title",{length:160}).notNull(), durationMinutes:integer("duration_minutes"), sortOrder:integer("sort_order").notNull() },t=>[uniqueIndex("test_sections_order_uq").on(t.testId,t.sortOrder)]);
export const testQuestions = pgTable("test_questions", { testId:uuid("test_id").references(()=>tests.id,{onDelete:"cascade"}).notNull(), sectionId:uuid("section_id").references(()=>testSections.id,{onDelete:"cascade"}).notNull(), questionId:uuid("question_id").references(()=>questions.id).notNull(), sortOrder:integer("sort_order").notNull() },t=>[primaryKey({columns:[t.testId,t.questionId]}),uniqueIndex("test_questions_order_uq").on(t.sectionId,t.sortOrder)]);
export const productTests = pgTable("product_tests", { productId:uuid("product_id").references(()=>products.id,{onDelete:"cascade"}).notNull(), testId:uuid("test_id").references(()=>tests.id,{onDelete:"cascade"}).notNull() },t=>[primaryKey({columns:[t.productId,t.testId]})]);
export const materials = pgTable("materials", { id:id(), examId:uuid("exam_id").references(()=>exams.id).notNull(), title:varchar("title",{length:200}).notNull(), type:materialType("type").notNull(), body:text("body"), privateObjectKey:text("private_object_key"), status:contentStatus("status").default("DRAFT").notNull(), createdBy:uuid("created_by").references(()=>users.id).notNull(), createdAt:createdAt(), updatedAt:updatedAt() });
export const productMaterials = pgTable("product_materials", { productId:uuid("product_id").references(()=>products.id,{onDelete:"cascade"}).notNull(), materialId:uuid("material_id").references(()=>materials.id,{onDelete:"cascade"}).notNull() },t=>[primaryKey({columns:[t.productId,t.materialId]})]);
export const orders = pgTable("orders", { id:id(), userId:uuid("user_id").references(()=>users.id).notNull(), status:orderStatus("status").default("CREATED").notNull(), subtotalPaise:integer("subtotal_paise").notNull(), discountPaise:integer("discount_paise").default(0).notNull(), totalPaise:integer("total_paise").notNull(), currency:varchar("currency",{length:3}).default("INR").notNull(), idempotencyKey:varchar("idempotency_key",{length:100}).notNull(), createdAt:createdAt(), updatedAt:updatedAt() },t=>[uniqueIndex("orders_idempotency_uq").on(t.idempotencyKey),index("orders_user_idx").on(t.userId,t.createdAt),check("orders_amounts_check",sql`${t.subtotalPaise} >= 0 and ${t.discountPaise} >= 0 and ${t.totalPaise} >= 0 and ${t.totalPaise} = ${t.subtotalPaise} - ${t.discountPaise}`)]);
export const orderItems = pgTable("order_items", { id:id(), orderId:uuid("order_id").references(()=>orders.id,{onDelete:"cascade"}).notNull(), productId:uuid("product_id").references(()=>products.id).notNull(), productName:varchar("product_name",{length:180}).notNull(), unitPricePaise:integer("unit_price_paise").notNull(), quantity:integer("quantity").default(1).notNull() });
export const payments = pgTable("payments", { id:id(), orderId:uuid("order_id").references(()=>orders.id).notNull(), provider:varchar("provider",{length:30}).notNull(), providerOrderId:varchar("provider_order_id",{length:150}), providerPaymentId:varchar("provider_payment_id",{length:150}), idempotencyKey:varchar("idempotency_key",{length:100}).notNull(), status:paymentStatus("status").default("CREATED").notNull(), amountPaise:integer("amount_paise").notNull(), currency:varchar("currency",{length:3}).default("INR").notNull(), verifiedAt:timestamp("verified_at",{withTimezone:true}), failureCode:varchar("failure_code",{length:100}), rawMetadata:jsonb("raw_metadata").$type<Record<string,unknown>>(), createdAt:createdAt(), updatedAt:updatedAt() },t=>[uniqueIndex("payments_idempotency_uq").on(t.idempotencyKey),uniqueIndex("payments_provider_payment_uq").on(t.provider,t.providerPaymentId),index("payments_order_idx").on(t.orderId),check("payments_amount_check",sql`${t.amountPaise} > 0`)]);
export const entitlements = pgTable("entitlements", { id:id(), userId:uuid("user_id").references(()=>users.id).notNull(), productId:uuid("product_id").references(()=>products.id).notNull(), orderId:uuid("order_id").references(()=>orders.id).notNull(), status:entitlementStatus("status").default("ACTIVE").notNull(), startsAt:timestamp("starts_at",{withTimezone:true}).defaultNow().notNull(), expiresAt:timestamp("expires_at",{withTimezone:true}).notNull(), revokedAt:timestamp("revoked_at",{withTimezone:true}), createdAt:createdAt() },t=>[uniqueIndex("entitlements_order_product_uq").on(t.orderId,t.productId),index("entitlements_access_idx").on(t.userId,t.productId,t.status,t.expiresAt),check("entitlements_window_check",sql`${t.expiresAt} > ${t.startsAt}`)]);
export const attempts = pgTable("attempts", { id:id(), userId:uuid("user_id").references(()=>users.id).notNull(), testId:uuid("test_id").references(()=>tests.id).notNull(), status:attemptStatus("status").default("CREATED").notNull(), sequence:integer("sequence").notNull(), startedAt:timestamp("started_at",{withTimezone:true}), serverDeadlineAt:timestamp("server_deadline_at",{withTimezone:true}), submittedAt:timestamp("submitted_at",{withTimezone:true}), createdAt:createdAt(), updatedAt:updatedAt() },t=>[uniqueIndex("attempts_user_test_seq_uq").on(t.userId,t.testId,t.sequence),index("attempts_test_status_idx").on(t.testId,t.status)]);
export const attemptAnswers = pgTable("attempt_answers", { attemptId:uuid("attempt_id").references(()=>attempts.id,{onDelete:"cascade"}).notNull(), questionId:uuid("question_id").references(()=>questions.id).notNull(), selectedOptionIds:jsonb("selected_option_ids").$type<string[]>().default([]).notNull(), textAnswer:text("text_answer"), numericAnswer:numeric("numeric_answer"), markedForReview:boolean("marked_for_review").default(false).notNull(), isCorrect:boolean("is_correct"), awardedMarks:numeric("awarded_marks",{precision:8,scale:2}), savedAt:timestamp("saved_at",{withTimezone:true}).defaultNow().notNull() },t=>[primaryKey({columns:[t.attemptId,t.questionId]})]);
export const bookmarks = pgTable("bookmarks", { userId:uuid("user_id").references(()=>users.id,{onDelete:"cascade"}).notNull(), questionId:uuid("question_id").references(()=>questions.id,{onDelete:"cascade"}).notNull(), createdAt:createdAt() },t=>[primaryKey({columns:[t.userId,t.questionId]})]);
export const webhookEvents = pgTable("webhook_events", { id:id(), provider:varchar("provider",{length:30}).notNull(), providerEventId:varchar("provider_event_id",{length:150}).notNull(), eventType:varchar("event_type",{length:100}).notNull(), signatureVerified:boolean("signature_verified").default(false).notNull(), payload:jsonb("payload").$type<Record<string,unknown>>().notNull(), processedAt:timestamp("processed_at",{withTimezone:true}), error:text("error"), createdAt:createdAt() },t=>[uniqueIndex("webhook_provider_event_uq").on(t.provider,t.providerEventId)]);
export const auditLogs = pgTable("audit_logs", { id:id(), actorUserId:uuid("actor_user_id").references(()=>users.id), action:varchar("action",{length:100}).notNull(), entityType:varchar("entity_type",{length:80}).notNull(), entityId:varchar("entity_id",{length:100}), requestId:varchar("request_id",{length:100}), ipHash:text("ip_hash"), before:jsonb("before"), after:jsonb("after"), createdAt:createdAt() },t=>[index("audit_entity_idx").on(t.entityType,t.entityId),index("audit_actor_idx").on(t.actorUserId,t.createdAt)]);
export const notifications = pgTable("notifications", { id:id(), userId:uuid("user_id").references(()=>users.id,{onDelete:"cascade"}).notNull(), type:varchar("type",{length:50}).notNull(), title:varchar("title",{length:180}).notNull(), body:text("body").notNull(), readAt:timestamp("read_at",{withTimezone:true}), createdAt:createdAt() },t=>[index("notifications_user_idx").on(t.userId,t.readAt,t.createdAt)]);
