import { requirePermission } from "@/features/auth/authorization";import { OPERATIONS_PERMISSIONS } from "@/features/operations/permissions";import { deliverNotification } from "@/features/operations/service";import { operationIdSchema } from "@/features/operations/validation";import { successResponse } from "@/lib/http/api-response";import { executeRoute } from "@/lib/http/route-handler";
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){return executeRoute(request,async requestId=>{await requirePermission(OPERATIONS_PERMISSIONS.manageNotifications);return successResponse(await deliverNotification(operationIdSchema.parse((await params).id),requestId),requestId);});}
