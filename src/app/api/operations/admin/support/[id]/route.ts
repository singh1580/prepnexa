import { requirePermission } from "@/features/auth/authorization";import { OPERATIONS_PERMISSIONS } from "@/features/operations/permissions";import { setManagedSupportTicket } from "@/features/operations/service";import { managedTicketUpdateSchema,operationIdSchema } from "@/features/operations/validation";import { successResponse } from "@/lib/http/api-response";import { executeRoute } from "@/lib/http/route-handler";
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){return executeRoute(request,async requestId=>{const auth=await requirePermission(OPERATIONS_PERMISSIONS.manageSupport);const input=managedTicketUpdateSchema.parse(await request.json());return successResponse(await setManagedSupportTicket(operationIdSchema.parse((await params).id),input,{userId:auth.user.id,requestId}),requestId);});}
